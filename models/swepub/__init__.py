from models.swepub.article import SwepubArticle
from models.swepub.language import SwepubLanguage

# This is a workaround for changing the class hierarchy after the identifiers were pickled.
# Pandas does not seem to support changing the namespace
